driver.navigate().refresh();
driver.get(driver.getCurrentUrl()); 

// Делаем тесты полностью на WEBe
// перегружаем страницу, т.к. не всегда письмо приходит вовремя и нужный элемент на странице не появляется...
